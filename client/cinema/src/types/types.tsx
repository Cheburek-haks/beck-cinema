export interface IPlaces {
    name: string;
    phone: number;
    occupied: boolean;
    id: number;
    _id: string;
    count: number;
}

export interface IUser {
    name: string;
    phone: number;
    email: string;
}
